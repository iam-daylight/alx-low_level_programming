#include <stdio.h>

/**
 * main - this is the main function
 * Return: 0
 */
int main(void)
{
char i, v;

for (i = 'a'; i <= 'z'; i++)

putchar (i);

for (v = 'A'; v <= 'Z'; v++)

putchar (v);
putchar ('\n');

return (0);
}

