#include "main.h"

/**
 * _isdigit - functioin that checks digits
 * @c: integer to be checked
 * Return: return 1 if it's a digit, return 0 if it is not
 */

int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
