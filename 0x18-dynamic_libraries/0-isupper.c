#include "main.h"

/**
 * _isupper - function for uppercase letter
 * @c: variable to check
 * Return: return 1 if uppercase, return 0 if not
 */

int _isupper(int c)
{
if (c >= 'A' && c <= 'Z')
{
return (1);
}
return (0);
}
